# TemocArcadeServer

These are .php files intended to run on an Apache 2 web server connecting the Unity arcade client to the MariaDB database.

In Ubuntu the default directory for these files is: /var/www/html/
